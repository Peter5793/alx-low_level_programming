#include "holberton.h"
#include <stdio.h>

/**
 * _strlen - return the length of the character
 * @s: specific string
 *
 * Return: Void
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; ++i)
{
}
return (i);
}

